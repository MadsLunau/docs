---
title: "Introduction"
description: "Parahelp is the platform for building your AI support agent."
---

Parahelp is an AI support solution built for fast-moving software companies. It integrates with your existing ticketing system and functions as a team member to resolve tickets end-to-end.

The platform consists of two agents:

The **Customer Agent** is customer-facing, with scoped tool connections and context to safely resolve customer issues, either autonomously or with human approval. See [Customer Agent](/customer-agent/introduction).

The **Internal Agent** (also referred to as just "Parahelp") is internal-facing, with broader connections and the ability to change the Customer Agent's configurations, incl. building secure tool connections. See [Internal Agent](/internal-agent/introduction).

## How you work with Parahelp

You do all of this in [tasks](/platform/tasks). It starts with an onboarding task, where the Internal Agent analyzes your last 500-1,000 resolved tickets along with your existing context to build the first version of your Customer Agent. From there, the two agents run a continuous improvement loop:

1. **Build.** You describe the support flow you want in natural language, and the Internal Agent builds the necessary scoped policies, knowledge, and tool connections. See [Build](/internal-agent/build).
2. **Test.** Before any configuration changes ship, the Internal Agent tests the change against your historical support tickets, and production tests verify your tool connections. See [Test](/internal-agent/test).
3. **Release.** Changes ship as releases you can review and revert. When shipped, the [Customer Agent](/customer-agent/introduction) immediately uses the updated configurations to resolve tickets end-to-end across your channels: email, live chat, and Slack. Nothing goes live without your approval. See [Release](/internal-agent/release).
4. **Identify gaps.** When the Customer Agent lacks context, it triggers the Internal Agent to investigate using its broader context and return with proposed changes, starting a new loop. Automations watch your tools and your queue, and the Internal Agent reports the impact of every release. See [Identify gaps](/internal-agent/identify-gaps).

The loop continuously improves your Customer Agent. It learns from the tickets it handles, from what your team ships across your connected tools, and from every release you publish.

Reviewing Parahelp for security or legal? Start with how we handle your data in [Settings → Privacy](/platform/settings#privacy) and [our trust page](https://trust.parahelp.com/).
